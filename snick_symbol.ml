open Snick_ast

type symbolKind =
    | SYM_LOCAL
    | SYM_PARAM_VAL
    | SYM_PARAM_REF

type symType =
    | SYM_BOOL
    | SYM_REAL
    | SYM_INT

(* Bound on an array symbol object *)
type bound = (int * int * int) (* (lower * upper * offset_size) *)

(* Bound on an array symbol object *)
type bounds = bound list

type symbol = (symbolKind * symType * expr * int * int * bool * bounds option)
(* 
typedef struct symbol_data {
    SymbolKind  kind;
    SymType     type;
    void        *sym_value;    ??????????????????????
    int         line_no;
    int         slot;
    BOOL        used;
    Bounds  *bounds;           ??????????????????????
} symbol;
 *)

type scope = (ident * (string, symbol) Hashtbl.t * param list * int * int)
(* 
 typedef struct scope_data {
    char *id;                   ?????????????????????
    void *table;                   ?????????????????????
    void *params;                   ?????????????????????
    int line_no;
    int next_slot;
} scope;
 *)

(* 
type sym_table = (Hashtbl.t * bool) *)
(* 
typedef struct symbol_table {
    void *table;                  ?????????????????/
    BOOL initialised;
} sym_table;
 *)